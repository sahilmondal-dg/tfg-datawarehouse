version: 2

sources:
  - name: utilizecore
    schema: bronze
    database: "{{ env_var('DBT_DATABASE') }}"
    description: "utilizecore raw source — landed by ingest_utilizecore.py. No transformations applied."
    tables:
      # TODO: replace with real table names on Day 1 of access
      - name: _placeholder
        description: "Raw _placeholder table from utilizecore."
