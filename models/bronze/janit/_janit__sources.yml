version: 2

sources:
  - name: janit
    schema: bronze
    database: "{{ env_var('DBT_DATABASE') }}"
    description: "janit raw source — landed by ingest_janit.py. No transformations applied."
    tables:
      # TODO: replace with real table names on Day 1 of access
      - name: _placeholder
        description: "Raw _placeholder table from janit."
