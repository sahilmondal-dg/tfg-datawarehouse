version: 2

sources:
  - name: adp
    schema: bronze
    database: "{{ env_var('DBT_DATABASE') }}"
    description: "adp raw source — landed by ingest_adp.py. No transformations applied."
    tables:
      # TODO: replace with real table names on Day 1 of access
      - name: _placeholder
        description: "Raw _placeholder table from adp."
