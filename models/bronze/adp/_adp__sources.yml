version: 2

sources:
  - name: adp
    schema: dbo
    database: "{{ env_var('DBT_DATABASE') }}"
    description: "ADP raw tables — written as Delta by ingest_adp_api.py. No transformations applied."
    tables:
      - name: bronze_adp_users
        description: "Raw ADP workers (DummyJSON /users in test mode)."
      - name: bronze_adp_products
        description: "Raw ADP pay statements (DummyJSON /products in test mode)."
