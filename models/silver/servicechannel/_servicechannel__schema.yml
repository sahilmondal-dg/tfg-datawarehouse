version: 2

models:
  # TODO: replace with real model names as staging models are built
  - name: stg_servicechannel__placeholder
    description: "Staged servicechannel placeholder. Normalised, typed, snake_case. Source-shaped — no cross-source joins."
    columns:
      - name: servicechannel_id
        description: "Source system primary key."
        tests:
          - not_null
          - unique
