version: 2

models:
  # TODO: replace with real model names as staging models are built
  - name: stg_utilizecore__placeholder
    description: "Staged utilizecore placeholder. Normalised, typed, snake_case. Source-shaped — no cross-source joins."
    columns:
      - name: utilizecore_id
        description: "Source system primary key."
        tests:
          - not_null
          - unique
